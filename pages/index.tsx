import * as React from 'react'

export default class EntryComponent extends React.Component {

  public render() {
    return (
      <div>
        {`NextJs + Typescript + Babel + React + Redux app motherfucker`}
      </div>
    )
  }
}
